[[valueexpressions.fundamentals]]
= Cơ sở về Value Expressions

Value Expressions là sự kết hợp giữa {spring-framework-docs}/core/expressions.html[Spring Expression Language (SpEL)] và {spring-framework-docs}/core/beans/annotation-config/value-annotations.html[Property Placeholder Resolution].
Chúng kết hợp khả năng evaluate mạnh mẽ các expression mang tính lập trình với sự đơn giản của việc dùng property-placeholder resolution để lấy các giá trị từ `Environment`, chẳng hạn như các configuration property.

Các expression nên được xác định bởi một trusted input, chẳng hạn như giá trị của annotation, thay vì lấy từ user input.

== Phạm vi

Value Expressions được sử dụng trong các annotation context.
Spring Data cung cấp khả năng evaluate Value Expression trong hai context chính:

* *Mapping Model Annotations*: chẳng hạn như `@Document`, `@Field`, `@Value` và các annotation khác trong những module Spring Data đi kèm với mapping model riêng cùng Entity Reader tương ứng, như MongoDB, Elasticsearch, Cassandra, Neo4j.
Các module xây dựng trên các thư viện cung cấp mapping model riêng (JPA, LDAP) không hỗ trợ Value Expressions trong các mapping annotation.
+
Đoạn mã sau minh họa cách sử dụng các expression trong context của mapping model annotation.
+
.Cách sử dụng annotation `@Document`
====
[source,java]
----
@Document("orders-#{tenantService.getOrderCollection()}-${tenant-config.suffix}")
class Order {
  // …
}
----
====

* *Repository Query Methods*: chủ yếu thông qua `@Query`.
+
Đoạn mã sau minh họa cách sử dụng các expression trong context của repository query methods.
+
.Cách sử dụng annotation `@Query`
====
[source,java]
----
class OrderRepository extends Repository<Order, String> {

  @Query("select u from User u where u.tenant = ?${spring.application.name:unknown} and u.firstname like %?#{escape([0])}% escape ?#{escapeCharacter()}")
  List<Order> findContainingEscaped(String namePart);
}
----
====

NOTE: Tham khảo tài liệu của module để xác định cú pháp binding tham số by-name/by-index thực tế.
Thông thường, các expression được thêm tiền tố bằng `:#{…}`/`:${…}` hoặc `?#{…}`/`?${…}`.

== Expression Syntax

Value Expressions có thể được định nghĩa từ một SpEL Expression đơn lẻ, một Property Placeholder hoặc một composite expression kết hợp nhiều loại expression, bao gồm cả literal.

.Các ví dụ về expression
====
[source]
----
#{tenantService.getOrderCollection()}                          <1>
#{(1+1) + '-hello-world'}                                      <2>
${tenant-config.suffix}                                        <3>
orders-${tenant-config.suffix}                                 <4>
#{tenantService.getOrderCollection()}-${tenant-config.suffix}  <5>
----

<1> Value Expression sử dụng một SpEL Expression đơn lẻ.
<2> Value Expression sử dụng một SpEL Expression tĩnh, được evaluate thành `2-hello-world`.
<3> Value Expression sử dụng một Property Placeholder đơn lẻ.
<4> Composite expression bao gồm literal `orders-` và Property Placeholder `${tenant-config.suffix}`.
<5> Composite expression sử dụng SpEL, Property Placeholder và literal.
====

NOTE: Việc sử dụng Value Expressions mang lại rất nhiều tính linh hoạt cho code của bạn.
Tuy nhiên, việc này yêu cầu expression phải được evaluate trong mỗi lần sử dụng và do đó, việc evaluate Value Expression ảnh hưởng đến performance profile.

{spring-framework-docs}/core/expressions/language-ref.html[Spring Expression Language (SpEL)] và {spring-framework-docs}/core/beans/annotation-config/value-annotations.html[Property Placeholder Resolution] giải thích chi tiết cú pháp và khả năng của SpEL và Property Placeholder.

[[valueexpressions.api]]
== Parsing và Evaluation

Value Expressions được parse bởi API `ValueExpressionParser`.
Các instance của `ValueExpression` là thread-safe và có thể được cache để dùng sau, nhằm tránh phải parse lặp lại.

Ví dụ sau minh họa cách sử dụng API của Value Expression:

.Parsing và Evaluation
[tabs]
======
Java::
+
[source,java,role="primary"]
----
ValueParserConfiguration configuration = SpelExpressionParser::new;
ValueEvaluationContext context = ValueEvaluationContext.of(environment, evaluationContext);

ValueExpressionParser parser = ValueExpressionParser.create(configuration);
ValueExpression expression = parser.parse("Hello, World");
Object result = expression.evaluate(context);
----

Kotlin::
+
[source,kotlin,role="secondary"]
----
val configuration = ValueParserConfiguration { SpelExpressionParser() }
val context = ValueEvaluationContext.of(environment, evaluationContext)

val parser = ValueExpressionParser.create(configuration)
val expression: ValueExpression = parser.parse("Hello, World")
val result: Any = expression.evaluate(context)
----
======

[[valueexpressions.spel]]
== SpEL Expressions

{spring-framework-docs}/core/expressions.html[SpEL Expressions] tuân theo Template style, trong đó expression được kỳ vọng nằm trong định dạng `#{…}`.
Các expression được evaluate bằng một `EvaluationContext` do `EvaluationContextProvider` cung cấp.
Bản thân context là một `StandardEvaluationContext` mạnh mẽ, cho phép thực hiện nhiều loại thao tác, truy cập các static type và các context extension.

NOTE: Đảm bảo chỉ parse và evaluate các expression từ những nguồn đáng tin cậy, chẳng hạn như annotation.
Việc chấp nhận các user-provided expression có thể tạo ra một đường vào để khai thác application context và hệ thống của bạn, dẫn đến một lỗ hổng bảo mật tiềm ẩn.

=== Mở rộng Evaluation Context

`EvaluationContextProvider` và biến thể reactive của nó là `ReactiveEvaluationContextProvider` cung cấp quyền truy cập vào một `EvaluationContext`.
`ExtensionAwareEvaluationContextProvider` và biến thể reactive của nó là `ReactiveExtensionAwareEvaluationContextProvider` là các triển khai mặc định xác định các context extension từ application context, cụ thể là `ListableBeanFactory`.

Các extension triển khai `EvaluationContextExtension` hoặc `ReactiveEvaluationContextExtension` để hydrate một `EvaluationContext` bằng một root object, các named properties và các functions (top-level methods).

Ví dụ sau minh họa một context extension cung cấp root object, các property, các function và một aliased function.

.Triển khai một `EvaluationContextExtension`
[tabs]
======
Java::
+
[source,java,role="primary"]
----
@Component
public class MyExtension implements EvaluationContextExtension {

    @Override
    public String getExtensionId() {
        return "my-extension";
    }

    @Override
    public Object getRootObject() {
        return new CustomExtensionRootObject();
    }

    @Override
    public Map<String, Object> getProperties() {

        Map<String, Object> properties = new HashMap<>();

        properties.put("key", "Hello");

        return properties;
    }

    @Override
    public Map<String, Function> getFunctions() {

        Map<String, Function> functions = new HashMap<>();

        try {
            functions.put("aliasedMethod", new Function(getClass().getMethod("extensionMethod")));
            return functions;
        } catch (Exception o_O) {
            throw new RuntimeException(o_O);
        }
    }

    public static String extensionMethod() {
        return "Hello World";
    }

    public static int add(int i1, int i2) {
        return i1 + i2;
    }

}

public class CustomExtensionRootObject {

	public boolean rootObjectInstanceMethod() {
		return true;
	}

}
----

Kotlin::
+
[source,kotlin,role="secondary"]
----
@Component
class MyExtension : EvaluationContextExtension {

    override fun getExtensionId(): String {
        return "my-extension"
    }

    override fun getRootObject(): Any? {
        return CustomExtensionRootObject()
    }

    override fun getProperties(): Map<String, Any> {
        val properties: MutableMap<String, Any> = HashMap()

        properties["key"] = "Hello"

        return properties
    }

    override fun getFunctions(): Map<String, Function> {
        val functions: MutableMap<String, Function> = HashMap()

        try {
            functions["aliasedMethod"] = Function(javaClass.getMethod("extensionMethod"))
            return functions
        } catch (o_O: Exception) {
            throw RuntimeException(o_O)
        }
    }

    companion object {
        fun extensionMethod(): String {
            return "Hello World"
        }

        fun add(i1: Int, i2: Int): Int {
            return i1 + i2
        }
    }
}

class CustomExtensionRootObject {
	fun rootObjectInstanceMethod(): Boolean {
		return true
	}
}
----
======

Sau khi extension ở trên được đăng ký, bạn có thể sử dụng các exported methods, properties và root object của nó để evaluate các SpEL expressions:

.Ví dụ về Expression Evaluation
====
[source]
----
#{add(1, 2)}                                             <1>
#{extensionMethod()}                                     <2>
#{aliasedMethod()}                                       <3>
#{key}                                                   <4>
#{rootObjectInstanceMethod()}                            <5>
----

<1> Gọi method `add` được khai báo bởi `MyExtension`, cho kết quả là `3` vì method này cộng hai numeric parameters và trả về tổng.
<2> Gọi method `extensionMethod` được khai báo bởi `MyExtension`, cho kết quả là `Hello World`.
<3> Gọi method `aliasedMethod`.
Method này được cung cấp dưới dạng function và chuyển tiếp đến method `extensionMethod` được khai báo bởi `MyExtension`, cho kết quả là `Hello World`.
<4> Evaluate property `key`, cho kết quả là `Hello`.
<5> Gọi method `rootObjectInstanceMethod` trên instance root object `CustomExtensionRootObject`.
====

Bạn có thể tìm thấy các context extension trong thực tế tại https://github.com/spring-projects/spring-security/blob/main/data/src/main/java/org/springframework/security/data/repository/query/SecurityEvaluationContextExtension.java[`SecurityEvaluationContextExtension`].

[[valueexpressions.property-placeholders]]
== Property Placeholders

Các Property Placeholder có dạng `${…}` thường tham chiếu đến các property được cung cấp bởi một `PropertySource` thông qua `Environment`.
Các property hữu ích khi được resolve dựa trên system properties, application configuration files, environment configuration hoặc các property source do secret management systems đóng góp.
Bạn có thể tìm thêm chi tiết về Property Placeholders trong {spring-framework-docs}/core/beans/annotation-config/value-annotations.html#page-title[tài liệu Spring Framework về cách sử dụng `@Value`].
